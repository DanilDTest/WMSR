Процедура ПКО_Спр_ИсторияСтатусовИнтернетЗаказов_Отправка_ПриОтправкеДанных(ДанныеИБ, ДанныеXDTO, КомпонентыОбмена, СтекВыгрузки)
	ДанныеXDTO.Вставить("ЗаказКлиента", ДанныеИБ.Отбор.ЗаказКлиента.Значение);
	ДанныеXDTO.Вставить("Период", ДанныеИБ.Отбор.Период.Значение);
	
	ТаблицаЗаписи = ДанныеИБ.Выгрузить();
	ТаблицаЗаписи.Колонки.Добавить("СтатусЗначениеПеречисления");
	ТаблицаЗаписи.Колонки.Добавить("ДокументРегистраторГУИД"); 
	
	Для каждого Запись из ТаблицаЗаписи Цикл
		Запись.СтатусЗначениеПеречисления = Запись.Статус;
		Если ЗначениеЗаполнено(Запись.ДокументРегистратор) Тогда
			Запись.ДокументРегистраторГУИД = Строка(Запись.ДокументРегистратор.УникальныйИдентификатор());
		КонецЕсли;	
	КонецЦикла;
	ДанныеXDTO.Вставить("Записи", ТаблицаЗаписи);
КонецПроцедуры
