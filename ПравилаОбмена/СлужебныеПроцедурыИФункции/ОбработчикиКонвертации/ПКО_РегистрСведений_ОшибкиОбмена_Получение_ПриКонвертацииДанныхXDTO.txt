Процедура ПКО_РегистрСведений_ОшибкиОбмена_Получение_ПриКонвертацииДанныхXDTO(ДанныеXDTO, ПолученныеДанные, КомпонентыОбмена)
	УниверсальнаяДатаОшибки = ДанныеXDTO.УниверсальнаяДатаОшибки;
	
	ПолученныеДанные.ДополнительныеСвойства.Вставить("УниверсальнаяДатаОшибки", ДанныеXDTO.УниверсальнаяДатаОшибки);
	ПолученныеДанные.ДополнительныеСвойства.Вставить("БазаВозникновенияОшибки", ДанныеXDTO.БазаВозникновенияОшибки);
	ПолученныеДанные.ДополнительныеСвойства.Вставить("БазаИсточникСообщения", ДанныеXDTO.БазаИсточникСообщения);
	
	ПолученныеДанные.ДополнительныеСвойства.Вставить("Записи", Неопределено);
	
	Если ДанныеXDTO.Свойство("Записи")
		И ЗначениеЗаполнено(ДанныеXDTO.Записи) Тогда
		ПолученныеДанные.ДополнительныеСвойства.Вставить("Записи", ДанныеXDTO.Записи);
	КонецЕсли;
КонецПроцедуры
