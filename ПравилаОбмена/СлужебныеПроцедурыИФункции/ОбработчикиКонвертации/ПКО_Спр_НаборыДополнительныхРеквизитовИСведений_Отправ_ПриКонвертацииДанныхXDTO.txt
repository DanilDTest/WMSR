Процедура ПКО_Спр_НаборыДополнительныхРеквизитовИСведений_Отправ_ПриКонвертацииДанныхXDTO(ДанныеXDTO, ПолученныеДанные, КомпонентыОбмена)
	НаименованиеНабора = ПолученныеДанные.Наименование;
	ПозицияКавычек = СтрНайти(НаименованиеНабора, """");
	Если НЕ ПозицияКавычек = 0 Тогда     
		ПозицияКавычекЗакрывающая = СтрНайти(НаименованиеНабора, """",,, 2);  
		КоличествоСимолов = ПозицияКавычекЗакрывающая - ПозицияКавычек - 1; 
	
		НаименованиеНабора = Сред(НаименованиеНабора, ПозицияКавычек + 1, КоличествоСимолов);
		ПолученныеДанные.Наименование = НаименованиеНабора;    
			
	КонецЕсли;
	
	                 
	                 
	Если ДанныеXDTO.Свойство("ИмяПредопределенных") Тогда
		ПолученныеДанные.XDTO.Вставить("ИмяПредопределенных", ДанныеXDTO.ИмяПредопределенных);  
	КонецЕсли;
КонецПроцедуры
