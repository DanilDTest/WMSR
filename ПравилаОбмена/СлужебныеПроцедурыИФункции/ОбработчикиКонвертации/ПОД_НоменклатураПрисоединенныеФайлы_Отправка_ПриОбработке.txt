Процедура ПОД_НоменклатураПрисоединенныеФайлы_Отправка_ПриОбработке(ДанныеИБ, ИспользованиеПКО, КомпонентыОбмена)
	ФайлыКартинок = РасширенияИзображений();
	
	Если ФайлыКартинок.Найти(ДанныеИБ.Расширение) = Неопределено Тогда
		ИспользованиеПКО.НоменклатураПрисоединенныеФайлы_Отправка = Ложь;
	КонецЕсли;
КонецПроцедуры
