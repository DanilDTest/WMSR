Процедура ПКО_Справочник_RM_ИзображенияНоменклатуры_Получение_ПриКонвертацииДанныхXDTO(ДанныеXDTO, ПолученныеДанные, КомпонентыОбмена)
	Номенклатура = ДанныеXDTO.Номенклатура;
	
	ПолученныеДанные.ДополнительныеСвойства.Вставить("Номенклатура", Номенклатура);  
	ПолученныеДанные.ДополнительныеСвойства.Вставить("Записи",   Неопределено);
	
	Если ДанныеXDTO.Свойство("Записи")
		И ЗначениеЗаполнено(ДанныеXDTO.Записи) Тогда
		ПолученныеДанные.ДополнительныеСвойства.Вставить("Записи", ДанныеXDTO.Записи);
	КонецЕсли;     
	
	Если ДанныеXDTO.Свойство("GUIDНоменклатураПрисоединенныеФайлы")
		И ЗначениеЗаполнено(ДанныеXDTO.GUIDНоменклатураПрисоединенныеФайлы) Тогда
		ПолученныеДанные.ДополнительныеСвойства.Вставить("GUIDНоменклатураПрисоединенныеФайлы", Новый УникальныйИдентификатор(ДанныеXDTO.GUIDНоменклатураПрисоединенныеФайлы));
	КонецЕсли;
КонецПроцедуры
