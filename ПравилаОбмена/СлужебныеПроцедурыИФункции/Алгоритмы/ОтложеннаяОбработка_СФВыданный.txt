Процедура ОтложеннаяОбработка_СФВыданный(Объект, КомпонентыОбмена)
	Если Не ДокументыОснованияСуществуют(Объект) Тогда
		Возврат;
	КонецЕсли;
	
	Объект.ЗаполнитьПараметрыСчетаФактурыПоОснованию();
	Объект.ПредставлениеНомера = Документы.СчетФактураВыданный.ПолучитьПредставлениеНомера(Объект);
КонецПроцедуры
