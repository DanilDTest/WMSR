Процедура ВыгрузитьПодразделение(ДанныеИБ, ДанныеXDTO, Подразделение = Неопределено, ИмяСвойстваXDTO = "Подразделение")
	Если Подразделение = Неопределено Тогда
		Подразделение = ДанныеИБ.Подразделение;
	КонецЕсли;
	
	Инструкция = Неопределено;
	Если ЗначениеЗаполнено(Подразделение) Тогда
		Инструкция = Новый Структура("Значение, ИмяПКО", Подразделение, "Справочник_Подразделения_Отправка");
	КонецЕсли;
	
	ДанныеXDTO.Вставить(ИмяСвойстваXDTO, Инструкция);
КонецПроцедуры
