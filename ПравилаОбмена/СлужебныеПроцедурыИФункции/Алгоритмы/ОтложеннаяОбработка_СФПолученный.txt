Процедура ОтложеннаяОбработка_СФПолученный(Объект, КомпонентыОбмена)
	Если Не ДокументыОснованияСуществуют(Объект) Тогда
		Возврат;
	КонецЕсли;
	
	Корректировочный = Объект.Корректировочный;
	Исправление = Объект.Исправление;
	
	Объект.ОбменДанными.Загрузка = Истина;
	Объект.ЗаполнитьПараметрыСчетаФактурыПоОснованию();
	Объект.ОбменДанными.Загрузка = Ложь;
	
	Объект.Корректировочный = Корректировочный;
	Объект.Исправление = Исправление;
КонецПроцедуры
