Процедура ПодменитьЗначенияРеквизитовПереводами(ПолученныеДанные, ДанныеXDTO, КомпонентыОбмена, ЗаменаНаименования)
	//ЯзыкКонфигурации = КомпонентыОбмена.ПараметрыКонвертации.ЯзыкКонфигурации;
	//ТаблицаПереводов = Неопределено;    
	
	//ДанныеXDTO.Свойство("Переводы", ТаблицаПереводов);
	
	//Если ТаблицаПереводов = Неопределено 
	//	ИЛИ НЕ ЗначениеЗаполнено(ЯзыкКонфигурации) Тогда 
	//	Возврат;
	//КонецЕсли;
	//     
	
	//ПереводыПоЯзыку = ТаблицаПереводов.НайтиСтроки(Новый Структура("ЯзыкКод", ЯзыкКонфигурации.Код));
	//СтруктураПереводов = Новый Структура;       
	//СтруктураОригинальныхРеквизитов = Новый Структура;
	//Для каждого СтрокаПеревода из ПереводыПоЯзыку Цикл 
	//	
	//	СтруктураПереводов.Вставить(СтрокаПеревода.Реквизит, СтрокаПеревода.Значение);
	//	СтруктураОригинальныхРеквизитов.Вставить(СтрокаПеревода.Реквизит, "");	
	//КонецЦикла;     
	//                        
	//ЗаполнитьЗначенияСвойств(СтруктураОригинальныхРеквизитов, ПолученныеДанные);                        
	//ЗаполнитьЗначенияСвойств(ПолученныеДанные, СтруктураПереводов);      
	
	//Если ЗаменаНаименования = Истина
	//	И СтруктураПереводов.Свойство("НаименованиеПолное") Тогда
	//	
	//	ПолученныеДанные.Наименование = СтруктураПереводов.НаименованиеПолное;
	//КонецЕсли;  
	
	//ПолученныеДанные.ДополнительныеСвойства.Вставить("СтруктураРеквизитовДоПеревода", СтруктураОригинальныхРеквизитов);
КонецПроцедуры
