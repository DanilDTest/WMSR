Функция ДанныеСтатусаЗаказаКлиента(СсылкаЗаказа) Экспорт
	СтатусПредставление = "";
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	RM_ИсторияСтатусовИнтернетЗаказовСрезПоследних.Статус КАК Статус,
		|	RM_ИсторияСтатусовИнтернетЗаказовСрезПоследних.Период КАК Период,
		|	RM_ИсторияСтатусовИнтернетЗаказовСрезПоследних.МестноеВремя КАК МестноеВремя
		|ИЗ
		|	РегистрСведений.RM_ИсторияСтатусовИнтернетЗаказов.СрезПоследних(, ЗаказКлиента = &ЗаказКлиента) КАК RM_ИсторияСтатусовИнтернетЗаказовСрезПоследних";
	
	Запрос.УстановитьПараметр("ЗаказКлиента", СсылкаЗаказа);
	
	РезультатЗапроса = Запрос.Выполнить();	
	Выборка = РезультатЗапроса.Выбрать();
	
	СтруктураРезультат = Новый Структура;    
	СтруктураРезультат.Вставить("СтатусРасширеныйПредставление");
	СтруктураРезультат.Вставить("СтатусРасширеныйПериод");
	СтруктураРезультат.Вставить("СтатусРасширеныйМестноеВремя");
	
	Пока Выборка.Следующий() Цикл
		СтатусЗначение = Выборка.Статус;
		                                      
		Если ЗначениеЗаполнено(СтатусЗначение) Тогда 
			СтруктураРезультат.СтатусРасширеныйПредставление = Строка(СтатусЗначение);
			СтруктураРезультат.СтатусРасширеныйПериод = Выборка.Период;        
			
			Если ЗначениеЗаполнено(Выборка.МестноеВремя) Тогда
				СтруктураРезультат.СтатусРасширеныйМестноеВремя = Выборка.МестноеВремя; 
			КонецЕсли;
	
		
		КонецЕсли;      
	КонецЦикла;  
	
	Возврат СтруктураРезультат;
КонецФункции
