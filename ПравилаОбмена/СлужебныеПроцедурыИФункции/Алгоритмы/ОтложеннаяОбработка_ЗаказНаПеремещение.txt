Процедура ОтложеннаяОбработка_ЗаказНаПеремещение(Объект, КомпонентыОбмена)
	ЗаполнитьСтатусыУказанияСерий(Объект, КомпонентыОбмена);
	ЗаполнитьВидДеятельностиПоУмолчанию(Объект, "ПеремещениеПодДеятельность");
	
	#Область ЗаполнениеОбеспечения
	
	СтруктураДействий = Новый Структура;
	СтруктураДействий.Вставить("ПроверитьЗаполнитьОбеспечение",
		Новый Структура("ЗаполнитьОбособленно", Ложь));
	СтруктураДействий.Вставить("ПроверитьЗаполнитьНазначение");
	
	Для Каждого Стр Из Объект.Товары Цикл
		//Если НЕ ЗначениеЗаполнено(Стр.ВариантОбеспечения) Тогда
		//	ОбработкаТабличнойЧастиСервер.ОбработатьСтрокуТЧ(Стр, СтруктураДействий, Неопределено);
		//КонецЕсли;
		Если НЕ ЗначениеЗаполнено(Стр.НачалоОтгрузки) Тогда
			Стр.НачалоОтгрузки = Объект.Дата;
		КонецЕсли;
			
		Если НЕ ЗначениеЗаполнено(Стр.ВариантОбеспечения) Тогда
			Стр.ВариантОбеспечения = Перечисления.ВариантыОбеспечения.Отгрузить; 
		КонецЕсли;
	КонецЦикла;
	
	#КонецОбласти
КонецПроцедуры
